server:
  port: 8181

spring:
  application:
    name: jpa-study

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    hibernate:
      # DDL ?? ?? ??
      # none: ???? ?? ?? (?? ??)
      # create: ?? ??? ?? ? ?? ??
      # create-drop: ?? ? ??? ??
      # update: ??? ???? ?? (?? ??)
      # validate: ???? ???? ?? ??????? ??
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true # SQL ?? ?? ??
        format_sql: true # ??? ???? ???













